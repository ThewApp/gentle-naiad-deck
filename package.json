{
  "name": "gentle-naiad-deck",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "mdx-deck deck.mdx --no-open",
    "build": "mdx-deck build deck.mdx --basepath=\"/$npm_package_name\"",
    "test": "npm run build"
  },
  "devDependencies": {
    "mdx-deck": "^2.3.2"
  }
}
{
  "name": "gentle-naiad-deck",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "mdx-deck deck.mdx --no-open",
    "build": "mdx-deck build deck.mdx --basepath=\"/$npm_package_name\" && mdx-deck-export pdf deck.mdx",
    "test": "npm run build"
  },
  "devDependencies": {
    "mdx-deck": "^2.3.2",
    "@mdx-deck/export": "^2.3.2"
  }
}
