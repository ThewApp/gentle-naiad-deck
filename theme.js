import { dark as defaultTheme } from '@mdx-deck/themes'

export default {
  ...defaultTheme,
  font: 'Lato, ' + defaultTheme.font,
  colors: {
    text: '#2de898',
    background: '#000'
  },
  googleFont: "https://fonts.googleapis.com/css?family=Lato&display=swap",
}
